import 'core';

macro statement(whitespace, 'if', expression/condition, block/true_clause) {
    return 'if {} {} else {{{}}}'.format(condition, true_clause);
}

if a {}

var clamp;
clamp = (a, x, b) -> {
    return x;
};

clamp.__name__ = 'clamp';

var x;
x = foo(1);
if le(x, 0) {
    loop {
        if not(x.pop()) {

        } else {}
        bar(baz(x), 4);
    }
} else {
    quix(kek);
}